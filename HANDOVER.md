# HANDOVER — Chess Quest

_Обновлено: 2026-06-14_

## Текущая ветка

`feat/level-3-tower` (up to date с `origin/feat/level-3-tower`)

## Последние коммиты

```
0235ac7 fix: restore RadialGradient to ChessSquare
2ca8532 polish: PNG pieces, board colors, perf optimizations, effects system
4a86137 docs: update handover
f738e65 feat/cburnett-gradient-pieces-and-board
1d94b1c fix/pieces-and-board-gradient
```

## Что реализовано

### Режим ХАОС (основной режим, заменил классическую башню)
Roguelite-башня: магазин → бои → элита/событие → магазин → босс.
Стейт — `chaosModeStore.ts` (армия, золото, артефакты, улучшения, этаж, очки).

### Уровень 1 — Лес Пешек
- Магазин 1 (150 золота, без ферзя) → Бой 1 (ELO 800) → Сокровище →
  Бой 2 (ELO 1000) → Магазин 2 → Босс «Всадник» (ELO 1400)
- Босс: ферзь-берсерк 🔴, ладья-страж 🔵, король спавнит коней каждый ход
- Армия НЕ переходит на уровень 2 (старт с нуля)

### Уровень 2 — Долина Коней
- Магазин 1: 120g (200g для Купца), без ферзя → Бой 1 (конь-берсерк, ELO 1000) →
  выбор маршрута: Элита 💀 (ELO 1300, гарантированное +событие) ИЛИ
  Бой 2 (ELO 1100, +Сокровище) → Бой 3 (ELO 1200) → Бой 4 (ELO 1300) →
  Магазин 2 → Босс «Двуглавый Рыцарь» (ELO 1500/1600)
- Босс: 2 коня-берсерка 🔴, ферзь-страж 🔵, во время боя один конь AI
  каждые 5 ходов получает улучшение «Снайпер», король спавнит коней
  (макс 4, каждые 2 хода)
- Армия и улучшения переходят на уровень 3

### Уровень 3 — Храм Диагоналей (текущая ветка)
- Магазин 1: 100g (200g для Купца), без ферзя → Бой 1 (слон-снайпер + конь-берсерк,
  ELO 1200) → выбор маршрута: Элита 💀 (ELO 1500, бонусный магазин) ИЛИ
  Бой 2 (2× снайпер + страж, ELO 1300, +Событие) → Бой 3 (ELO 1400) →
  Бой 4 (3× атакующих + 2× страж, ELO 1500) → Элита обязательная 💀
  (ELO 1600, 5 улучшений) → Магазин 2 → Босс «Ведьма Диагоналей» (ELO 1800)
- Босс: стартовый полный комплект чёрных + 2 слона-снайпера 🔴,
  конь-берсерк 🔴, ферзь-страж 🔵. Механика телепортации — каждые
  5 ходов атакующие фигуры (снайперы + берсерк) телепортируются на
  случайные свободные клетки рядов 5–8, король и ферзь-страж остаются.
  Счётчик под доской: «🌀 Телепортация через: N ходов»
- Реализовано: `chaosLevelConfig.ts`, `chaosTowerConfig.ts`,
  телепортация подключена в `chaos-battle.tsx`, экран входа к боссу
  (`chaos-boss-intro.tsx`), переход армии/улучшений с уровня 2

### Персонажи (герои)
- Пешка Тимми — аура +25% золота за превращения
- Рыцарь Финн — аура +30% золота за вилки коня
- Купец — стартует с 200 золота на каждом уровне (базовое золото
  уровня игнорируется); реализовано в `chaos-victory.tsx`
  (`startingGoldForNextLevel`)

### Случайные события (GDD §22)
12 событий в 3 уровнях (положительные 40% / нейтральные 35% /
негативные 25%), 60% шанс срабатывания после обычного боя.
`chaosEvents.ts` + `chaosEventEngine.ts` + экран `chaos-event.tsx`.

### Дизайн / графика
- Фигуры RhosGFX (SVG, Cburnett-стиль)
- Белые фигуры получили линейный градиент (`wGrad`/`wAccent`)
- Клетки доски — `radialGradient` через `react-native-svg`
  (заменил `LinearGradient`)

## Что сейчас в работе

- **PNG-фигуры**: `ChessPiece.tsx` теперь рендерит PNG (Cburnett,
  перекрашены, `src/assets/pieces/*.png`) через `Image` вместо `SvgXml`.
  `fadeDuration={0}` + `React.memo` с кастомным компаратором
  (`pieceKey`/`size`) — устраняет мигание при перерендере доски.
- **Анимация слайда фигур временно убрана**: `usePieceAnimation.ts`
  удалён, `ChessBoard.tsx` делает ход синхронно через `attemptMove`
  без промежуточной анимации (см. Known issues).
- **Клетки доски (`ChessSquare.tsx`)**: отрисовываются через
  `RadialGradient` (`react-native-svg`), cx/cy 50%, rx/ry 70%,
  `gradientUnits="objectBoundingBox"` — светлая `#d4e0f9`→`#b8bbf7`,
  тёмная `#8973f6`→`#7a66f4`. ID градиента уникален на инстанс
  (`useId()`, без `:`). `React.memo` сохранён. Временный
  flat-color перф-тест завершён, `RadialGradient` восстановлен
  (коммит `0235ac7`).
- **Перф-оптимизации в `chaos-battle.tsx`**: обновление стейта после
  хода игрока и хода ИИ отложено через `requestAnimationFrame` —
  не блокирует немедленную отрисовку перемещённой фигуры.
- **Система эффектов фигур**: добавлен `pieceEffects` (тип
  `PieceEffect` в `src/types/pieceEffects.ts`) в `chaosModeStore` —
  универсальные эффекты (улучшения + дебаффы), работает параллельно
  с `pieceUpgrades`. `ChaosUpgradeChips.tsx` переработан под
  переименованные улучшения.
- ELO противника в шапке боя (`eloBadge`) и кнопка возврата
  (`backBtn`, `‹`) на экране `chaos-battle.tsx`.

## Что делать дальше

1. Баг берсерк-коня (нестабильное/некорректное поведение улучшения
   `berserk` у коней — проверить `chaosAIUpgrades.ts`)
2. Улучшения ИИ не отображаются в HUD — добавить индикацию
   AI-улучшений (берсерк/снайпер/страж) в интерфейс боя
3. Защита от дублирования улучшений — не давать повторно применить
   уже купленное улучшение к той же фигуре
4. Восстановить анимацию слайда фигур (после диагностики лага —
   см. Known issues), используя удалённый `usePieceAnimation.ts`
   как основу
5. Анимация спавна коней боссом (визуальный эффект появления новых
   коней при механике спавна босса Уровня 1)

## Known issues

- Анимация слайда фигур отключена (см. "Что делать дальше" п.4) —
  ходы применяются мгновенно, без промежуточного движения фигуры.
- На физических устройствах изредка (~1 ход из 10) наблюдается
  лаг/задержка рендера хода — точная причина не подтверждена,
  подозревается GC (сборка мусора). Тест с заменой `RadialGradient`
  на плоский цвет в `ChessSquare.tsx` завершён без однозначного
  вывода, `RadialGradient` восстановлен (коммит `0235ac7`) — причину
  лага нужно искать в другом месте.
